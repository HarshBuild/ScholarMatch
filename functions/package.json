{
  "name": "scholarship-matcher-functions",
  "description": "Firebase Cloud Functions + ingestion logic for Scholarship Matcher",
  "private": true,
  "type": "module",
  "engines": { "node": "20" },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^4.9.0"
  }
}
